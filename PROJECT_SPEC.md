# DairyGate 生鲜乳罐批入厂前联合检验闭环

## 项目目标

DairyGate 是面向乳品加工厂原奶检验室的单节点 Go 后端与浏览器检验控制台，用于管理一次生鲜乳罐批入厂前联合检验。系统在建检时锁定牧场编号、乳罐批号、罐仓隔舱、封签编号、冷链温度记录器、盲码样本、三联分管数量、抑制板孔位、培养箱时段、温度采样窗口、抗生素阈值、微生物阈值、理化阈值、复核人员名单和任务代次；随后经过双人采样确认、盲码分管、板孔与培养箱限时占用、冷链覆盖核验、抗生素抑制圈读数、体细胞与菌落计数复测、冰点掺水疑点复判、两名合格人员独立复核，最终只形成可入厂、已入厂、质量隔离或已取消中的唯一终局。SQLite WAL 持久化目录、任务聚合、占用账簿、冷链覆盖格、不可覆盖证据、仪器调用、幂等结果、审计事件和终局凭据，支持确定性重启恢复。规模设计为 50-58 个生产 Go 文件、约 5200-6200 行有效生产 Go 代码，覆盖至少 catalog、inspection、blindcode、occupancy、evidence、arbiter、store、api、cmd、webembed 等包。

## 端到端业务流程

1. 建检：创建乳罐批检验任务并一次性冻结牧场、隔舱、封签、记录器、盲码、孔位、培养箱时段、阈值、温度窗口、复核人员名单和任务代次。
2. 双人采样确认：两名具备资格且角色不重合的人员分别确认牧场、乳罐批号、隔舱、封签和操作号，内容相同重试返回同一结果，内容冲突稳定拒绝。
3. 盲码三联分管：每个隔舱样本拆分为三联分管，建立乳罐批号与盲码的一次性映射闸门以及隔舱样本三联一致性矩阵，提前揭盲或重复盲码拒绝。
4. 资源占用与冷链核验：抑制板孔位和培养箱时段以限时占用账簿原子锁定；温度记录器按锁定时点写入覆盖格，并用整数规则计算连续超温分钟。
5. 检测与复测：抗生素读板仪、体细胞计数仪、菌落培养批次和理化读数均生成不可覆盖证据；固定小数位整数运算处理阈值边界、长度、符号、除零和溢出。
6. 复判与复核：疑阳、污染、冷链断点或三联结果分歧只创建一个带代次复判证据，复判覆盖受影响盲码、隔舱和孔位；两名不同合格复核人通过后才允许终局竞争。

## 核心组件与职责

1. 牧场与原奶检验规则目录：维护牧场、罐仓隔舱、封签作用域、记录器型号、人员资质、仪器能力、阈值版本和规则生效代次。
2. 乳罐批次检验任务聚合：实现状态机、任务代次锁、幂等操作号、终局栅栏、审计事件和重启恢复重放。
3. 盲码样本与板孔培养箱占用账簿：维护盲码唯一映射、三联分管一致性、抑制板孔位占用、培养箱时段占用、释放条件和并发裁定。
4. 冷链温度及理化采集账簿：维护温度窗口覆盖格、记录器摘要、冰点/脂肪/蛋白固定点读数、派生值和算术失败隔离。
5. 抗生素与微生物复判仲裁器：处理抑制圈阈值边界、体细胞和菌落复测、培养批次污染、复判代次隔离、迟到读数和最终质量结论。
6. Go HTTP API 与浏览器检验控制台：提供 JSON API、静态资源嵌入、真实可操作页面、package.json、锁文件和固定构建脚本。

## 领域规则与不变量

1. 状态仅允许按待建检、待采样确认、盲码分管中、板孔占用中、冷链核验中、抗生素读数中、微生物培养中、理化复测中、待独立复核、可入厂、已入厂、质量隔离、已取消推进。
2. 开放任务中，同一乳罐批号、封签编号、盲码、抑制板孔位、培养箱时段只能绑定到一个有效任务；建检、换孔和启动均在同一事务内原子裁定。
3. 乳罐批号与盲码映射只能建立一次；揭盲必须处于允许状态并匹配当前代次，旧代次读数不可覆盖当前证据。
4. 隔舱样本三联分管必须数量一致、盲码一致、隔舱一致；任一分管缺失、重复或读数分歧进入复判路径。
5. 温度覆盖格只接受锁定窗口内、唯一时点、合法范围的记录；记录器摘要冲突、缺测和重复时点拒绝整批写入。
6. 抗生素、微生物和理化证据一经有效写入不得覆盖，只能追加带代次的复判证据；终态后任何普通操作均稳定拒绝。
7. 仪器拒绝、断连、超时和格式错误只能形成待重试调用记录；占用不提前释放，合格结论不由失败调用推导。
8. 采样人员、复核人员和复判人员的角色约束由锁定规则目录判定，同一人在不允许重合的角色上推进任务必须拒绝。

## 数据模型与持久化

1. farms：牧场编号、名称、有效期、允许罐仓隔舱、封签作用域、规则版本。
2. raw_milk_rules：抗生素抑制圈阈值、体细胞阈值、菌落阈值、冰点阈值、脂肪蛋白阈值、温度窗口、人员资质要求、版本摘要。
3. inspection_tasks：任务 ID、牧场编号、乳罐批号、隔舱集合、封签集合、记录器编号、任务代次、状态、创建逻辑时间、终局类型。
4. idempotency_records：任务 ID、操作号、操作类型、请求摘要、响应摘要、稳定错误码、逻辑时间。
5. blind_samples：任务 ID、乳罐批号、隔舱、盲码、三联管序号、映射状态、揭盲代次。
6. resource_occupancies：任务 ID、资源类型、板号、孔位、培养箱、时段起止、占用代次、释放逻辑时间。
7. temperature_cells：任务 ID、记录器编号、时点、温度定点整数、窗口序号、覆盖状态、摘要片段。
8. evidence_records：任务 ID、盲码、隔舱、孔位、证据类型、原始定点值、派生值、阈值版本、证据代次、不可覆盖标记。
9. instrument_calls：调用 ID、任务 ID、仪器类型、目标盲码或孔位、脚本结果、重试次数、下一次逻辑时间、错误分类。
10. reviews_and_final_decisions：任务 ID、复核人、资质快照、复核结论、终局凭据、唯一竞争令牌。

## 公开接口

1. POST /api/tasks：建检并冻结全部规则与资源候选，返回任务代次、初始状态和占用摘要。
2. POST /api/tasks/{id}/sampling-confirmations：提交双人采样确认，支持操作号幂等和内容冲突检测。
3. POST /api/tasks/{id}/blind-splits：写入三联分管矩阵并建立盲码闸门。
4. POST /api/tasks/{id}/occupancies：原子占用或更换抑制板孔位与培养箱时段。
5. POST /api/tasks/{id}/cold-chain/readings：批量写入温度点并计算覆盖格与连续超温分钟。
6. POST /api/tasks/{id}/readings：提交抗生素、体细胞、菌落、冰点、脂肪、蛋白读数或仪器调用回执。
7. POST /api/tasks/{id}/rejudgements 与 POST /api/tasks/{id}/reviews：创建复判证据并提交独立复核。
8. POST /api/tasks/{id}/finalize：竞争生成可入厂、已入厂、质量隔离或已取消终局；GET /api/tasks/{id} 返回完整任务快照供浏览器检验控制台展示。

## 失败边界

1. 每个业务命令在 SQLite 事务内读取任务代次、幂等记录、占用账簿和状态，任一校验失败整体回滚。
2. 拒绝响应包含稳定错误码，并按牧场、乳罐批号、盲码、孔位、温度时点排序返回确定原因。
3. 并发建检、孔位占用、培养箱时段占用、最终结论竞争使用唯一索引和 compare-and-set 状态更新共同裁定。
4. 温度点、分管记录、仪器读数和派生证据采用先验证后写入，失败事务不得留下部分覆盖格、部分分管或部分证据。
5. 仪器调用失败只追加可审计调用记录和重试计划；重试次数、目标对象和逻辑时间由测试可控时钟决定。
6. 重启恢复从持久化任务、占用、幂等、证据和审计事件重建内存索引，不依赖进程内临时状态。
7. 终态栅栏优先于全部普通命令，迟到读数、旧代次复判、旧代次复核和重复终局均不改变已持久化状态。

## 验收标准

1. 建检时固定牧场编号、乳罐批号、罐仓隔舱、封签编号、冷链记录器、盲码样本、三联分管数量、抑制板孔位、培养箱时段、温度采样窗口、抗生素阈值、微生物阈值、理化阈值和任务代次。
2. 每个乳罐批号、封签编号、盲码、抑制板孔位和培养箱时段在开放任务中只能被一个有效检验任务占用，并发建检、换孔或启动必须原子裁定。
3. 采样确认、分管、揭盲与复核必须匹配当前任务代次，相同操作号同内容重试幂等而内容冲突报错，错误牧场、错误隔舱或陈旧规则不得推进状态。
4. 冷链核验必须覆盖锁定温度时点并使用确定整数规则计算连续超温分钟，缺测、重复时点、越界温度和记录器摘要冲突不得写入有效覆盖格。
5. 抗生素抑制圈、体细胞、菌落、冰点、脂肪和蛋白读数全部使用固定小数位整数运算并检查长度、符号、除零和溢出，算术失败不得写入派生证据。
6. 体细胞计数仪、培养箱探头或抗生素读板仪拒绝、断连、超时、格式错误只能形成可审计待重试调用，重试对象、次数和逻辑时间可确定，不得伪造合格或提前释放占用。
7. 出现抗生素疑阳、菌落污染、冷链断点或三联结果分歧时只能创建一个带代次的复判证据，复判须覆盖受影响盲码、隔舱和孔位，旧代次迟到读数保持不可覆盖且不影响当前结论。
8. 全部温度覆盖格完整、抗生素阴性、微生物与理化指标满足锁定阈值，并由两名不同且合格人员独立复核后，竞争成功者才可生成唯一入厂凭据；并发入厂、质量隔离与取消只能形成一个终局，终态后的读数、复判、复核或普通操作一律被拒绝且不改变状态。

## 确定性测试场景

1. catalog_rules_test.go：虚构牧场、隔舱、封签作用域、人员资质和规则版本锁定；覆盖错配、重复封签、陈旧规则拒绝。
2. inspection_idempotency_test.go：采样确认、分管、揭盲和复核的任务代次匹配；覆盖同操作号同内容重试、内容冲突、角色重合。
3. occupancy_concurrency_test.go：同步屏障制造并发建检、换孔、培养箱时段竞争；断言只有一个任务获得占用且失败方无残留。
4. blind_split_matrix_test.go：三联分管数量、一致性矩阵、盲码重复、提前揭盲和隔舱错写均返回确定错误码。
5. cold_chain_coverage_test.go：固定温度时点、缺测、重复、越界和摘要冲突；断言连续超温分钟的整数计算结果。
6. fixed_point_readings_test.go：抗生素抑制圈阈值边界、体细胞和菌落计数溢出、冰点符号与位数、脂肪蛋白除零路径。
7. instrument_retry_test.go：可控读板仪、计数仪和培养箱探头脚本化拒绝、断连、超时、格式错误；断言重试对象、次数和逻辑时间。
8. rejudgement_generation_test.go：疑阳、污染、冷链断点和三联分歧只创建一个代次复判证据，旧代次迟到读数不可覆盖。
9. finalization_race_test.go：并发可入厂、质量隔离、取消竞争；断言唯一终局、终态后所有普通操作稳定拒绝。
10. restart_recovery_test.go：在采样后、占用后、读数后、复核前分别重启服务，断言状态、幂等结果、占用和证据完全恢复。
11. api_contract_test.go：HTTP JSON 错误码、原因排序、状态快照字段和浏览器控制台静态资源可访问。
12. public_fixture_test.go：使用固定乳罐批号、盲码、孔位、温度时点、整数计数和定点理化读数跑通合格入厂路径。

## 组件追踪关系

1. 牧场与原奶检验规则目录 -> catalog 包，覆盖 farm repository、rule snapshot、person qualification、seal scope、threshold parser、fixtures。
2. 乳罐批次检验任务聚合 -> inspection 包，覆盖 state machine、generation guard、idempotency service、audit appender、final barrier、recovery loader。
3. 盲码样本与板孔培养箱占用账簿 -> blindcode 与 occupancy 包，覆盖 blind gate、split matrix、plate ledger、incubator ledger、lease conflict detector。
4. 冷链温度及理化采集账簿 -> evidence 包，覆盖 temperature grid、fixed-point decimal parser、derived metric calculator、immutable evidence writer。
5. 抗生素与微生物复判仲裁器 -> arbiter 包，覆盖 inhibition reader rules、microbial recount rules、contamination generation, late evidence filter、decision preconditions。
6. Go HTTP API 与浏览器检验控制台 -> api、cmd、webembed 和 web 目录，覆盖 JSON handlers、middleware、static embed、package.json、package-lock.json、deterministic build script。

## 独特性

The project centers on dairy raw-milk intake science: blind-coded tank samples, compartment split tubes, inhibition plates, incubator slots, cold-chain recorder coverage, microbial counts, fixed-point physicochemical thresholds, qualified lab reviewers, and one auditable plant-entry decision for a fresh milk tank batch.
